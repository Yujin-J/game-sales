{
  "name": "game-sales-table",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check scripts/build-pages.mjs && node --check scripts/sync-rankings.mjs",
    "build:pages": "node scripts/build-pages.mjs",
    "sync:rankings": "node scripts/sync-rankings.mjs"
  },
  "dependencies": {
    "google-play-scraper": "^10.1.3"
  },
  "engines": {
    "node": ">=20"
  }
}
